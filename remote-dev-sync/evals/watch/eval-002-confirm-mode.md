# Eval: 确认模式监控

## 任务

用户希望修改代码后手动确认再同步编译。

指导用户使用确认模式（默认模式）。

## 期望输出

回答应：
1. 展示 watch 命令（不加 --auto）
2. 说明确认模式交互
3. 说明 s 跳过、q 退出
4. 说明适用场景

## 验证要点

- [ ] python scripts/remote_dev.py watch -p <name>
- [ ] 检测变化后等待确认
- [ ] 回车确认、s 跳过、q 退出
- [ ] 适合攒多个修改一起编译
