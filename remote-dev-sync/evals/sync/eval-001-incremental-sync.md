# Eval: 增量同步代码

## 任务

用户改了几个文件，需要同步到远程重新编译。项目之前已配置过，配置在 ~/.config/remote-dev/ 里。

指导用户执行增量同步。

## 期望输出

回答应：
1. 展示读取已有配置
2. 展示 rsync 增量同步命令
3. 说明只同步变更文件
4. 展示远程执行编译

## 验证要点

- [ ] python scripts/remote_dev.py sync -p <name>
- [ ] 使用 rsync 增量同步
- [ ] 只同步变更文件
- [ ] 同步后可继续 build
