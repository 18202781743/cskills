# 验证计划

本 skill 的接口依据是用户提供的浏览器 HAR，另以 2026-09-17 实际填报纠错核对工作内容的跨周显示；考勤字段依据用户指定的桌面 HAR。没有对应公开头文件或服务端源码。验证时只读取接口结构和非敏感状态值，不输出 token、Cookie、工号或原始响应。

| 项目 | 验证方式 | 结果 |
| --- | --- | --- |
| OA 进入 BI 工时页面的路径 | 对照 HAR 的请求 URL 路径 | PASS |
| 读取接口、方法和参数名 | 对照 HAR 的 request method、URL query/body keys | PASS |
| 保存与提交均调用 `/sysworkhour/save` | 对照两次 POST | PASS |
| 保存 `status=0`，提交 `status=1` | 对照两次请求体及随后周查询 | PASS |
| 提交复用保存后的 ID | 对照保存后周查询和提交请求 | PASS |
| 成功响应 `code=0` 且需回读状态 | 对照两次响应及随后查询 | PASS |
| 默认工作内容可能跨周沿用，目标周文本须过滤旧日期 | 对照实际填报纠错中保存前后的文本和日期 | PASS |
| 一日内容只写该日，工作日预览与完整周接口日期分开 | 对照 2026-09-07 示例与周一/周日字段定义 | PASS |
| 保存前组装完整信息，草稿回读后报告结果 | 对照 `SKILL.md` 默认流程第 4-5 步和接口记录中的结果模板 | PASS |
| 默认执行路径仅使用 API，不依赖截图或坐标点击 | 对照 `SKILL.md` 与接口记录中的 API 顺序 | PASS |
| 已提交更正须经已验证撤回接口 | 对照 HAR 不含撤回接口及 skill 的限制说明 | PASS |
| OA 考勤月历接口方法和表单字段 | 对照桌面 HAR 的 POST 路径及 `typevalue`、`loaddata`、`type` | PASS |
| 每日打卡、工作日和请假字段 | 对照月历响应的 `date`、`isWorkDay`、`signInfo`、`workflow` 结构 | PASS |
| 9 小时下限与 12 小时加班门槛 | 运行 `python -m unittest discover -s tests -v`，覆盖 17:59、18:00、20:59、21:00、21:05 | PASS |
| 缺卡、请假、非工作日、跨日不自动填报 | 运行同一测试，核对 `status=review` | PASS |
| 2026-09-07 考勤样本与拟填 8.0 小时 | 将桌面 HAR 的月历响应经内存管道交给计算脚本；仅核对计算结果 | PASS |
| “帮我写今天的周报”触发完整 OA 填报流程 | 对照 `SKILL.md` 触发描述和口语化周报请求章节 | PASS |
| 同日多项工作按日期整理且不虚构成果 | 对照 V20 / deepin-home 示例及按周工作内容规则 | PASS |
| 同日多个事项记为一日总工时，不按事项拆小时 | 对照用户 2026-09-17 纠正及 `SKILL.md` 第 2 步、接口记录“按周工作内容” | PASS |
| 未指定改任务时沿用此前确认的任务，不按内容关键词换任务 | 对照用户 2500u2 实例及 `SKILL.md` 第 2 步、接口记录“按周工作内容” | PASS |
| 信息完整后本轮直接保存草稿，不询问是否保存；提交须草稿回读后单独确认 | 对照 `SKILL.md` 第 5-6 步、接口记录的 API 顺序及评测样例 10、12 | PASS |
| 保存前读取并合并已有工作内容，写入只允许追加且回读不得丢失旧内容 | 对照 2026-09-17 实际填报纠错记录及 `SKILL.md` 的追加、回读要求 | PASS |
| 目标周文本过滤其他周日期，保留本周旧条目 | 对照用户跨周纠正、`SKILL.md` 第 4 步和接口记录“按周工作内容” | PASS |
| 重复保存报错时先定位已有记录，不因单次空列表断言数据库不一致 | 对照用户提供的 `code=500` 报错与空列表、接口记录“重复记录排查” | PASS |
| 工时无法由有效考勤可靠计算时必须由用户指定，不猜测 | 对照 `SKILL.md` 输入和缺项、考勤异常规则 | PASS |
| 项目任务不唯一、当日缺下班卡或多任务工时未分配时不写入 | 对照口语化周报请求章节和考勤异常规则 | PASS |
| 接口记录与入口文档的保存及提交授权一致 | 对照 `SKILL.md` 默认流程第 5-6 步和接口记录的 API 执行顺序：直接保存并回读、提交另行确认 | PASS |
| skill 包内引用路径可解析，入口文档不依赖本机 HAR 路径 | 检查 `SKILL.md` 相对链接、接口记录的来源说明和文件存在性 | PASS |
| 触发范围覆盖口语化周报、补填和草稿，排除纯文字润色及其他 OA 待办 | 检查 `description` 与 `evals/trigger-evals.json` 五个样例；样例格式经 JSON 校验，实际模型触发效果待运行评测 | PASS |

在线 API 验收时，使用用户提供的测试工时或获得授权的真实工时，依次验证：当前会话认证、考勤月历与请假状态、项目任务唯一匹配、跨周文本过滤、信息完整后本轮直接保存草稿、保存前后的工作内容追加合并与回读保留、草稿回读后提交确认门槛、保存后回读草稿 ID、提交后回读状态 1、重复执行不重复追加、异常日期不自动猜测工时、失败时不提交。已提交更正的撤回 API 尚未从 HAR 验证；未验证前不得调用。未进行在线 API 写入前，不声称 API 端到端已通过。
